# 分阶段 EMA + 中等强度直接加权实验。
# EMA 从 epoch 3 开始生效时，立即将样本权重范围设为 [0.6, 1.0]。
experiment:
  name: condtc_qd_s3_m07_w06_direct
  output_root: runs

data:
  dataset: qdTimeNoise0424
  batch_size: 32
  initialization_batch_size: 256

model:
  num_clusters: 12

loss:
  time_loss_weight: 0.1
  clustering_loss_weight: 0.5
  instance_temperature: 0.5
  cluster_temperature: 1.0
  instance_loss_weight: 0.0
  cluster_contrastive_loss_weight: 0.0

optimizer:
  representation_learning_rate: 0.00005
  clustering_learning_rate: 0.00015
  weight_decay: 0.01

target_history:
  enabled: true
  start_epoch: 3
  momentum: 0.7
  # 低可靠性样本至少保留 60% 的 DEC 权重。
  minimum_weight: 0.6
  # EMA 启用后立即应用目标权重下限。
  weight_warmup: false
  save_interval: 1
  save_raw_assignments: true

training:
  num_epochs: 6
  seed: 19721013
  log_interval: 50
  max_initialization_batches: null
  max_train_batches: null

checkpoint:
  pretrain_path: checkpoints/sttraj2vec_pretrain_paper_15ep.pt
